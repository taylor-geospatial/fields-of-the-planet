trainer:
  max_epochs: 100
  log_every_n_steps: 10
  accelerator: gpu
  precision: bf16-mixed
  default_root_dir: logs/prue/ftw_joint-unet-efnet3-crop512-v3-augmax-full-s2init
  devices: [0]
  logger:
    class_path: lightning.pytorch.loggers.WandbLogger
    init_args:
      entity: isaaccorley
      project: ftw-planet
      name: joint-efnet3-crop512-v3-augmax-full-s2init
      save_dir: logs/prue/ftw_joint-unet-efnet3-crop512-v3-augmax-full-s2init
      log_model: false
  callbacks:
    - class_path: lightning.pytorch.callbacks.ModelCheckpoint
      init_args:
        monitor: val/loss
        mode: min
        save_top_k: 1
        save_last: true
        filename: "{epoch}-{val/loss:.2f}"
# Identical to ftw_joint_efnet3_crop512_v3_augmax_full.yaml but submitted
# with --ckpt_path pointing at the CCBY S2 PRUE-B3 checkpoint so the model
# is initialised from S2-trained weights before joint fine-tuning.
# Submit via: CKPT_PATH=<path> sbatch scripts/slurm/train_joint.sbatch
model:
  class_path: ftw_planet.trainers.FTWPlanetSegTask
  init_args:
    class_weights: [0.05, 0.2, 0.75]
    loss: "logcoshdice"
    model: "unet"
    backbone: "efficientnet-b3"
    weights: true
    patch_weights: false
    in_channels: 8
    num_classes: 3
    num_filters: 64
    ignore_index: 3
    lr: 1e-3
    patience: 100
data:
  class_path: ftw_planet.datamodules.FTWJointDataModule
  init_args:
    batch_size: 32
    num_workers: 10
    crop_size: 512
    s2_crop_size: 256
    load_boundaries: true
    usable_only: true
    planet_weight: 0.5
    preprocess_aug: true
    resize_aug: true
    swap_order: true
    per_band_gamma_aug: true
    per_band_affine_aug: true
    gaussian_blur_aug: true
    gaussian_noise_aug: true
    single_window_dropout_p: 0.15
    small_angle_rotation_aug: true
    shear_aug: true
    boundary_jitter_aug: true
    gsd_channel: false
    train_countries: [austria, belgium, brazil, cambodia, corsica, croatia, denmark, estonia, finland, france, germany, india, kenya, latvia, lithuania, luxembourg, netherlands, portugal, rwanda, slovakia, slovenia, south_africa, spain, sweden, vietnam]
    val_countries: [austria]
    test_countries: [austria, belgium, brazil, cambodia, corsica, croatia, denmark, estonia, finland, france, germany, india, kenya, latvia, lithuania, luxembourg, netherlands, portugal, rwanda, slovakia, slovenia, south_africa, spain, sweden, vietnam]
    s2_train_countries: [austria, belgium, brazil, cambodia, corsica, croatia, denmark, estonia, finland, france, germany, india, kenya, latvia, lithuania, luxembourg, netherlands, portugal, rwanda, slovakia, slovenia, south_africa, spain, sweden, vietnam]
  dict_kwargs:
    root: data
    s2_root: data/ftw
seed_everything: 7
